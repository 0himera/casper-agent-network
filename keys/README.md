# Keys Directory

Place your Casper account private key (`secret_key.pem`) in this directory for local runtime / validator / admin use.

> **SECURITY NOTICE**: Never commit `*.pem` or `*.key` files to version control.
